{% extends 'base.html.twig' %}

{% block title %}Ajoute une nouvele section{% endblock %}

{% block body %}
    <h1>Ajoute une nouvele section</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <button class="btn btn-success">Ajoute</button>
    {{ form_end(form) }}

    <a href="{{ path('section_index') }}">Revien vers sections</a>
{% endblock %}
