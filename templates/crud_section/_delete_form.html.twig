<form method="post" action="{{ path('app_crud_section_delete', {'id': section.id}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer ce commentaire?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ section.id) }}">
    <button class="btn">Supprimer</button>
</form>
