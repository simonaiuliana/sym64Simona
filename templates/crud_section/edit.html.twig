{% extends 'base.html.twig' %}

{% block title %}Modifier Section {{ section.sectionTitle }}{% endblock %}

{% block body %}
    <!-- Page Header-->
    <header class="masthead">
        <div class="container position-relative px-4 px-lg-5">
            <div class="row gx-4 gx-lg-5 justify-content-center">
                <div class="col-md-10 col-lg-8 col-xl-7">
                    <div class="site-heading">
                        <h1>Modifier Section {{ section.sectionTitle }}</h1>
                    </div>
                </div>
            </div>
        </div>
    </header>

    <div class="container px-4 px-lg-5">
        <div class="row gx-4 gx-lg-5 justify-content-center">
            <div class="col-md-10 col-lg-8 col-xl-7">
                <h1>Modifier Section</h1>
                {{ include('crud_section/_form.html.twig', {'button_label': 'Modifier'}) }}
                <a href="{{ path('app_crud_section_index') }}">Retour à la liste</a>
                {{ include('crud_section/_delete_form.html.twig') }}
            </div>
        </div>
    </div>
{% endblock %}
