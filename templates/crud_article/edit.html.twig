{% extends 'base.html.twig' %}

{% block title %}Modifier Article{% endblock %}

{% block body %}
    <!-- Page Header-->
    <header class="masthead">
        <div class="container position-relative px-4 px-lg-5">
            <div class="row gx-4 gx-lg-5 justify-content-center">
                <div class="col-md-10 col-lg-8 col-xl-7">
                    <div class="site-heading">
                        <h1>Modifier Le Article</h1>
                    </div>
                </div>
            </div>
        </div>
    </header>

    <div class="container-fluid px-4 px-lg-5">
        <div class="row gx-4 gx-lg-5 justify-content-center">
            <div class="col-md-10 col-lg-8 col-xl-7">
                <div class="container mt-3">
                    <h1>Modifier Le Article</h1>
                </div>
                {% if "ROLE_ADMIN" in user.roles %}
                {{ include('crud_article/_form.html.twig', {'button_label': 'Modifier'}) }}
                {% else %}
                {{ include('crud_article/_redac_form.html.twig', {'button_label': 'Modifier'}) }}
                {% endif %}
                <div class="container mt-3">
                    <a href="{{ path('app_crud_article_index') }}">Retour à la liste</a>
                    {{ include('crud_article/_delete_form.html.twig') }}
                </div>
            </div>
        </div>
    </div>
{% endblock %}
